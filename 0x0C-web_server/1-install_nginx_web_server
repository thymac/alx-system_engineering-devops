#!/bin/bash

# Update package sources
sudo apt-get update

# Install Nginx
sudo apt-get install -y nginx

# Start Nginx service
sudo service nginx start

# Configure Nginx to listen on port 80 and return "Hello World!" on root (/) request
sudo bash -c 'echo "Hello World!" > /var/www/html/index.html'
